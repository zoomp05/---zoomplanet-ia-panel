import React from 'react';
import { Typography, Alert } from 'antd';
const { Title } = Typography;

export default () => (
  <div>
    <Title level={3}>Crear Grupo de Anuncios</Title>
    <Alert message="Página en desarrollo" type="info" showIcon />
  </div>
);
